# Google Sheets API 설정
# 인증 방법 (아래 중 하나 선택):
# 1. 파일 경로 사용 (우선순위 1)
GOOGLE_CREDENTIALS_PATH=../../credentials.json
# 2. Python 호환 - 파일 경로 (우선순위 2)
# GOOGLE_CREDENTIALS_FILE=../../credentials.json
# 3. Python 호환 - JSON 문자열 직접 입력 (배포 환경용, 우선순위 3)
# GOOGLE_CREDENTIALS_JSON={"type": "service_account", ...}

# 스프레드시트 식별 (아래 중 하나 선택):
# 1. 스프레드시트 이름으로 찾기 (기본값)
SPREADSHEET_NAME=감귤 주문서(응답)
# 2. 스프레드시트 ID로 직접 접근 (선택 사항, 더 빠름)
# SPREADSHEET_ID=your_spreadsheet_id_here

# 기본 발송인 정보
DEFAULT_SENDER_ADDRESS=제주도 제주시 정실3길 113 C동 301호
DEFAULT_SENDER_NAME=안세진
DEFAULT_SENDER_PHONE=010-6395-0618

# 환경
NODE_ENV=development

# PostgreSQL 연결 정보 (Phase 2.1+)
DATABASE_URL=postgresql://user:password@localhost:5432/mytangerine?schema=public
